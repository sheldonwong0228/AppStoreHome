export default class Application{
    constructor(
        id,
        summary,
        name,
        artist,
        category,
        url
    ){
        this.id = id
        this.summary = summary
        this.name = name
        this.artist = artist
        this.category = category
        this.url = url
    }
}